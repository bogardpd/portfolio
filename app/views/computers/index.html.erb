<% provide(:title, "Computers") %>
<% provide(:meta_description, "Tech specs of Paul Bogard’s computers and other electronics.") %>
<% provide(:admin_actions, format_admin_actions({"Add Computer" => new_computer_path})) %>

<h1>Computers</h1>

<ul>
  <% @computers.each do |computer| %>
    <li><%= link_to(computer.name, computer_path(computer.slug)) %></li>
  <% end %>
</ul>