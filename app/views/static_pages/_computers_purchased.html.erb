<p class="purchased<% if(Date.parse(purchase_date) > Time.now.to_date-1.year) %> recent<% end %>">Purchased <abbr title="Purchased <%= format_date(Date.parse(purchase_date)) %>"><%= time_ago_in_words(purchase_date)%> ago</abbr></p>