<% provide(:title, "#{@parts.type_name} - Device History")%>
<% provide(:meta_description, "Paul Bogard’s device history for #{@parts.type_name(meta: true)}.") %>

<h1>Device History &ndash; <%= @parts.type_name %></h1>

<div class="center">
  <%= @parts.svg_xml %>
</div>