<% provide(:title, "iOS and OpenStreetMap GPS Logging Tutorial")%>
<% provide(:meta_description, "Paul Bogard’s tutorial for creating route maps with iOS devices and OpenStreetMap.")%>

<h1>iOS &amp; OpenStreetMap GPS Logging Tutorial</h1>

<div class="byline">Part of the <%= link_to "GPS logging", gps_logging_path %> project</div>

<p>iOS devices have plenty of apps that make use of location data, so it’s not surprising that it’s easy to record that location data over time. You can then use that data to show other people where you’ve been using <%= link_to("uMap", 'http://umap.openstreetmap.fr/en/') %> with OpenStreetMap layers.</p>

<h2>Overview</h2>

<p>The general process of creating a GPS map is as follows:</p>

<ul>
	<li><%= link_to "Record GPS routes with your iOS device", '#record' %></li>
	<li><%= link_to "Import the location data into uMap", '#import' %></li>
</ul>

<%= render 'static_pages/gps_logging_tutorials/ios_to_gpx', other_gps_path: 'gps_logging_garmin_osm_path' %>

<%= render 'static_pages/gps_logging_tutorials/gpx_to_osm', other_map_path: 'gps_logging_ios_google_earth_path' %>

<%= link_to("Return to GPS Logging".html_safe, gps_logging_path, class: "btn btn-default btn-block icon-button", role: "button") %>