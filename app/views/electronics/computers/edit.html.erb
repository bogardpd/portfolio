<% provide(:title, "Edit #{@computer.name}") %>
<% provide(:button_text, "Update computer") %>

<h1>Edit <%= @computer.name %></h1>

<%= render partial: "form" %>

<div class="row center">
  <div class="col-md-8 offset-md-2">
    <%= link_to "Delete this computer", electronics_computer_path(@computer), method: :delete, class: "btn btn-danger", data: { confirm: "Are you sure you want to delete #{@computer.name}?" }, role: "button" %>
  </div>
</div>
